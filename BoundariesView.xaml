<Window x:Class="Fisherman.BoundariesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fisherman"
        mc:Ignorable="d"
        Title="BoundariesView" Height="450" Width="800"
        AllowsTransparency="True" 
        WindowStyle="None"
        >
    <Window.Background>
        <SolidColorBrush Color="White" Opacity="0.2"></SolidColorBrush>
    </Window.Background>
    <Grid x:Name="gd">
        <Canvas Width="{Binding ElementName=gd, Path=ActualWidth}"
      Height="{Binding ElementName=gd, Path=ActualHeight}" x:Name="canvas" MouseDown="Canvas_MouseDown" MouseMove="Canvas_MouseMove" MouseUp="Canvas_MouseUp" Background="Transparent"/>
    </Grid>
</Window>
